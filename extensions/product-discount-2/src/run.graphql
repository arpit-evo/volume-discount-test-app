query RunInput {
  cart {
    lines {
      id
      quantity
    }
  }
  discountNode {
    metafield(
      namespace: "$app:discount-test"
      key: "function-configuration"
    ) {
      value
    }
  }
}
